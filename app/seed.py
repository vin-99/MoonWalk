from app.extenstions import db
from app.models.chef import Chef
from app.models.menu import Menu
from app.models.restaurant import Restaurant

def seed_data():

    # res1 = Restaurant(name = "MoonWalk", location = "Delhi")
    # res2 = Restaurant(name = "MoonWalk", location = "Mumbai")
    # res3 = Restaurant(name = "MoonWalk", location = "Bangalore")

    # db.session.add_all([res1, res2, res3])
    # db.session.commit()

    # chef1 = Chef(
    # name="Chef Ram",
    # is_available=True,
    # restaurant_id=res1.id)

    # chef2 = Chef(
    #     name="Chef Shyam",
    #     is_available=True,
    #     restaurant_id=res1.id)

    # chef3 = Chef(
    #     name="Chef Amit",
    #     is_available=True,
    #     restaurant_id=res2.id)

    # chef4 = Chef(
    #     name="Chef Raj",
    #     is_available=True,
    #     restaurant_id=res2.id)

    # chef5 = Chef(
    #     name="Chef Vijay",
    #     is_available=True,
    #     restaurant_id=res3.id)

    # chef6 = Chef(
    #     name="Chef Karan",
    #     is_available=True,
    #     restaurant_id=res3.id)

    # db.session.add_all([
    #     chef1,
    #     chef2,
    #     chef3,
    #     chef4,
    #     chef5,
    #     chef6
    # ])

    # db.session.commit()

    # menu1 = Menu(
    # dish_name="Pizza",
    # preperation_time=20,
    # restaurant_id=res1.id)

    # menu2 = Menu(
    #     dish_name="Burger",
    #     preperation_time=10,
    #     restaurant_id=res1.id)

    # menu3 = Menu(
    #     dish_name="Pasta",
    #     preperation_time=25,
    #     restaurant_id=res1.id)
    
    # menu4 = Menu(
    # dish_name="Pizza",
    # preperation_time=18,
    # restaurant_id=res2.id)

    # menu5 = Menu(
    #     dish_name="Pav Bhaji",
    #     preperation_time=15,
    #     restaurant_id=res2.id)

    # menu6 = Menu(
    #     dish_name="Vada Pav",
    #     preperation_time=8,
    #     restaurant_id=res2.id)
    
    # menu7 = Menu(
    # dish_name="Masala Dosa",
    # preperation_time=12,
    # restaurant_id=res3.id)

    # menu8 = Menu(
    #     dish_name="Idli",
    #     preperation_time=6,
    #     restaurant_id=res3.id)

    # menu9 = Menu(
    #     dish_name="Pizza",
    #     preperation_time=22,
    #     restaurant_id=res3.id)
    
    # db.session.add_all([menu1, menu2, menu3, menu4, menu5, menu6, menu7, menu8, menu9])
    # db.session.commit()
    pass